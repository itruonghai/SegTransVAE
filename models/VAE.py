import torch 
import torch.nn as nn

class VAE(nn.Module):
    def __init__(self, in_channels, latent_dim, in_dim, out_dim):
        super().__init__()
        


    def forward(self, x):   
        return x 